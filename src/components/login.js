import React from "react";
import styled from "styled-components";

const Login = props => {
    return (
        <div>
            <form>
                <h1>LOGIN</h1>
            </form>
        </div>
    );
};

export default Login;